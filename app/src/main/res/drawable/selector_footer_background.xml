<!-- textview_selector.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el TextView está presionado -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="8dp" /> <!-- Bordes redondeados -->
            <!-- Incluye tu shape personalizado para el color de fondo presionado -->
            <item android:drawable="@drawable/ovalo" />
        </shape>
        <item android:textStyle="bold" /> <!-- Aquí se aplica el estilo -->
    </item>
    <!-- Estado por defecto -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <!-- Define otros atributos de forma según sea necesario -->
        </shape>
    </item>
</selector>
